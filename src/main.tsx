
import { createRoot } from 'react-dom/client'
import App from './App.tsx'
import './index.css'

// Initialize React app with optimized rendering
createRoot(document.getElementById("root")!).render(<App />);
